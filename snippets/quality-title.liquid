<div class="quality-title quality-title-{{ index }}">
    <div class="quality-left-icon">
        <svg width="23" height="23" xmlns="http://www.w3.org/2000/svg">
            <g fill="none" fill-rule="evenodd">
                <path fill="{{ settings.title_color }}" d="M11 0v22l11-11z" />
                <path d="M.707 11L11 21.293 21.293 11 11 .707.707 11z" stroke="{{ settings.title_color }}" />
            </g>
        </svg>
    </div>
    <h2 class="quality-text pc-block">{{ settings.pc_title }}</h2>
    <h2 class="quality-text mb-block">{{ settings.mb_title }}</h2>
    <div class="quality-right-icon">
        <svg width="23" height="23" xmlns="http://www.w3.org/2000/svg">
            <g fill="none" fill-rule="evenodd">
                <path fill="{{ settings.title_color }}" d="M11 0v22l11-11z" />
                <path d="M.707 11L11 21.293 21.293 11 11 .707.707 11z" stroke="{{ settings.title_color }}" />
            </g>
        </svg>
    </div>
</div>
<style>
    .quality-title-{{ index }} {
        display: flex;
        font-weight: 600;
        font-size: 48px;
        line-height: 48px;
        color: {{ settings.title_color }};
        justify-content: center;
        align-items: start;
        text-align: center;
    }
    .quality-title-{{ index }} .quality-text{
        margin: 0 24px 16px;
        max-width: 800px;
    }
    .quality-title-{{ index }} .quality-right-icon svg{
        transform: rotate(180deg);
    }
    @media screen and (min-width: 1024px) and (max-width: 1439.98px) {
        .quality-title-{{ index }} {
            font-size: 3.3333333vw;
            line-height: 3.3333333vw;
        }
        .quality-title-{{ index }} .quality-text{
            margin: 0 1.666666667vw 16px;
            max-width: 55.555555556vw
        }
    }
    @media screen and (max-width: 1023.98px) {
        .quality-title-{{ index }} {
            font-weight: bold;
            font-size: 7.466666667vw;
            line-height: 9.6vw;
            margin-right: 4.26666666vw;
        }
        .quality-title-{{ index }} .quality-text{
            margin: 0 0 16px;
            width: 87.2vw;
        }
        .quality-title-{{ index }} .quality-right-icon,.quality-left-icon{
            display: none;
        }
    }
</style>
