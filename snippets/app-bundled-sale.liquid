{% comment %}
    产品详情页 - 添加捆绑销售
    Accepts: product
    Usage: {% render 'app-bundled-sale' %}
{% endcomment %}

{% comment %} open_bundled_sale {% endcomment %}
{%- if settings.open_bundled_sale == true -%}
  {% assign current_variant = product.selected_or_first_available_variant %}

  <div class="w-full product-bundled-sale-wrap" style="display:none;">
    <div
      id="app-bundled-sale"
      data-product-title="{{ product.title | escape }}"
      data-variant-id="{{ current_variant.id }}"
      data-variant-title="{{ current_variant.title | escape }}"
    ></div>

    {{ 'plugin-app-bundled-sale.min.css' | asset_url | stylesheet_tag }}
    <script src="{{ 'plugin-app-bundled-sale.min.js' | asset_url }}" defer></script>
  </div>
{% endif %}
