<div class="h-full flex items-center">
    {% if settings.cart_type == 'page' %}
        <a href="{{ routes.cart_url }}" class="header-actions-item header-actions--cart relative px-3" id="cart-icon-v5-menu-top">
    {% else %}
        <div class="header-actions-item header-actions--cart relative px-3" id="cart-icon-v5-menu-top">
    {% endif %}
        {%- render 'icon-cart', width: 29, height: 22, color: 'var(--v5-top-menu-text)' -%}
        {%- if cart != empty -%}
            <div style="left: unset;" class="cart-count-bubble absolute rounded-lg flex text-xs h-4 min-w-4 bg-black justify-center items-center right-2 -top-1">
                {%- if cart.item_count < 100 -%}
                    <span aria-hidden="true">{{ cart.item_count }}</span>
                {%- else -%}
                    <span aria-hidden="true">99+</span>
                {%- endif -%}
                <span class="visually-hidden">{{ 'sections.header.cart_count' | t: count: cart.item_count }}</span>
            </div>
        {%- endif -%}
    {% if settings.cart_type == 'page' %}
        </a>
    {% else %}
        </div>
    {% endif %}
</div>