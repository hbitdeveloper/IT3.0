{%- if section.settings.show_locale_switcher == true -%}
  {% comment %} IP定位 {% endcomment %}
  {% render 'locale-switcher-new' %}
{%- endif -%}
{% schema %}
{
  "name": "V5-IP定位",
  "enabled_on": {
    "groups": ["header"]
  },
  "limit": 1,
  "settings": [
    { "type": "checkbox", "id": "show_locale_switcher", "label": "IP定位", "default": false, "info": "是否展示IP定位" }
  ],
  "presets": [
    {
      "name": "V5-IP定位",
      "category": "V5.0",
      "settings": {
        "show_locale_switcher": false
      }
    }
  ]
}
{% endschema %}
