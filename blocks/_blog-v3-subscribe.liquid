{% doc %} 
    博客 - 订阅
{% enddoc %}
<div class="w-full mb-12 lg:mb-0" {{ block.shopify_attributes }}>
    {% render 'subscribe-v1', id: block.id, tag: block.settings.tag, klaviyo_id: block.settings.klaviyo_id %}
</div>

{% schema %}
    {
        "name": "博客聚合页 - 订阅",
        "tag": null,
        "settings": [
            { "type": "text", "id": "tag", "label": "订阅标签" },
            { "type": "text", "id": "klaviyo_id", "label": "klaviyo ID", "info": "如果配置则调用klaviyo的接口，没有则调用后端接口" },
        ],
        "presets": [
            {
                "name": "博客聚合页 - 订阅",
                "category": "Blog-V3"
            }
        ]
    }
{% endschema %}
